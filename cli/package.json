{
  "name": "@ralfie/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "ralf": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc && npm run postbuild",
    "postbuild": "mkdir -p dist/skills && cp src/skills/*.md dist/skills/ && if [ -d ../ui/dist ]; then mkdir -p dist/ui && cp -r ../ui/dist/* dist/ui/; fi && printf '#!/usr/bin/env node\\n' | cat - dist/index.js > dist/index.tmp.js && mv dist/index.tmp.js dist/index.js && chmod +x dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
