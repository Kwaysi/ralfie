{
  "project": "ralfie",
  "description": "CLI tool that manages agentic coding loops with planning, execution, tracking, and a real-time React dashboard",
  "created": "2026-03-18",
  "items": [
    {
      "id": "SCAFFOLD-1",
      "category": "Project Scaffolding",
      "description": "Create monorepo root with package.json (workspaces: cli, ui, shared), tsconfig.base.json, and .gitignore",
      "steps_to_verify": [
        "package.json exists at project root with 'workspaces' field containing ['cli', 'ui', 'shared']",
        "tsconfig.base.json exists with target ES2022, module Node16, strict true",
        ".gitignore includes node_modules/, dist/, and .ralfie/"
      ],
      "passes": true
    },
    {
      "id": "SCAFFOLD-2",
      "category": "Project Scaffolding",
      "description": "Create shared package with package.json and tsconfig.json",
      "steps_to_verify": [
        "shared/package.json exists with name '@ralfie/shared', type 'module'",
        "shared/tsconfig.json extends ../tsconfig.base.json",
        "shared/package.json has build and typecheck scripts"
      ],
      "passes": true
    },
    {
      "id": "SCAFFOLD-3",
      "category": "Project Scaffolding",
      "description": "Create CLI package with package.json (commander, ws deps) and tsconfig.json, bin entry 'ralf'",
      "steps_to_verify": [
        "cli/package.json exists with name '@ralfie/cli', type 'module', bin.ralf pointing to dist/index.js",
        "cli/package.json has commander and ws as dependencies, vitest as devDependency",
        "cli/tsconfig.json extends ../tsconfig.base.json"
      ],
      "passes": true
    },
    {
      "id": "SCAFFOLD-4",
      "category": "Project Scaffolding",
      "description": "Create UI package with package.json (React, Vite, Tailwind deps), tsconfig.json, and vite.config.ts with proxy settings",
      "steps_to_verify": [
        "ui/package.json exists with react, react-dom, react-router-dom, recharts, react-markdown as dependencies",
        "ui/vite.config.ts configures React and Tailwind plugins, proxies /api and /ws to localhost:3333",
        "ui/tsconfig.json has jsx: 'react-jsx' and module: 'ESNext'"
      ],
      "passes": true
    },
    {
      "id": "SCAFFOLD-5",
      "category": "Project Scaffolding",
      "description": "Install all dependencies and verify workspace typecheck passes",
      "steps_to_verify": [
        "Running 'npm install' succeeds without errors",
        "Running 'npm run typecheck' passes for all workspaces"
      ],
      "passes": true
    },
    {
      "id": "TYPES-1",
      "category": "Shared Types",
      "description": "Define shared TypeScript types: ItemStatus, PrdItem, PrdItemComment, Prd, BoardMeta, Board, RalfieConfig, WsEvent, WsEventType",
      "steps_to_verify": [
        "shared/src/types.ts exports ItemStatus as union type: 'pending' | 'in_progress' | 'done' | 'failed' | 'verified'",
        "PrdItem interface includes id, category, description, steps_to_verify (string[]), status, assigned_to (string|null), comments",
        "RalfieConfig interface includes agent_command, default_iterations, feedback_loops (string[]), serve_port",
        "WsEventType includes board:updated, prd:updated, progress:updated, lock:acquired, lock:released, run:started, run:iteration, run:completed",
        "Running 'npm run build --workspace=shared' produces shared/dist/types.js and shared/dist/types.d.ts"
      ],
      "passes": true
    },
    {
      "id": "LIB-1",
      "category": "CLI Libraries",
      "description": "Implement paths.ts module with helper functions for all .ralfie/ directory paths (ralfieDir, configPath, boardsDir, boardDir, planPath, prdPath, progressPath, locksDir, lockPath)",
      "steps_to_verify": [
        "cli/src/lib/paths.ts exports ralfieDir, configPath, boardsDir, boardDir, planPath, prdPath, progressPath, locksDir, lockPath",
        "ralfieDir returns '<cwd>/.ralfie'",
        "boardDir returns '<cwd>/.ralfie/boards/<boardName>'",
        "lockPath returns '<cwd>/.ralfie/boards/<boardName>/locks/<itemId>.lock'"
      ],
      "passes": true
    },
    {
      "id": "LIB-2",
      "category": "CLI Libraries",
      "description": "Implement config.ts module with readConfig, writeConfig, and defaultConfig for .ralfie/config.json",
      "steps_to_verify": [
        "readConfig returns defaultConfig when no config file exists",
        "writeConfig creates .ralfie directory if needed and writes config.json",
        "readConfig merges partial saved config with defaults (missing fields get default values)",
        "defaultConfig has agent_command='claude', default_iterations=10, feedback_loops=[], serve_port=3333",
        "All 3 config tests pass: 'npm test --workspace=cli -- --run config'"
      ],
      "passes": true
    },
    {
      "id": "LIB-3",
      "category": "CLI Libraries",
      "description": "Implement prd.ts module with readPrd, writePrd, claimItem, completeItem, failItem, verifyItem, addComment — all mutations use O_EXCL file locking for concurrent safety",
      "steps_to_verify": [
        "claimItem sets status to 'in_progress' and assigned_to to the session ID",
        "claimItem throws when item is already in_progress",
        "completeItem sets status to 'done' and clears assigned_to",
        "failItem sets status to 'failed', clears assigned_to, and appends a comment with the failure reason",
        "verifyItem sets status to 'verified' only when current status is 'done', throws otherwise",
        "addComment appends a PrdItemComment with timestamp, session_id, and message",
        "All PRD mutations use file locking via O_EXCL to prevent concurrent clobber",
        "All 8 PRD tests pass: 'npm test --workspace=cli -- --run prd'"
      ],
      "passes": true
    },
    {
      "id": "LIB-4",
      "category": "CLI Libraries",
      "description": "Implement board.ts module with createBoard, listBoards, getBoard, boardExists, appendProgress for board CRUD operations",
      "steps_to_verify": [
        "createBoard creates board directory with plan.md, prd.json, progress.md (empty), meta.json, and locks/ subdirectory",
        "boardExists returns true when board directory exists, false otherwise",
        "getBoard reads and returns meta, plan, prd, and progress data",
        "listBoards returns array of BoardMeta for all boards with meta.json files",
        "appendProgress appends text to progress.md",
        "All 4 board tests pass: 'npm test --workspace=cli -- --run board'"
      ],
      "passes": true
    },
    {
      "id": "LIB-5",
      "category": "CLI Libraries",
      "description": "Implement lock.ts module with acquireLock (O_EXCL atomic create), releaseLock, isLocked, getLockInfo, clearStaleLocks for per-item lockfile management",
      "steps_to_verify": [
        "acquireLock creates a .lock file atomically using O_EXCL, returns true on success",
        "acquireLock returns false (not throws) when lock already exists",
        "releaseLock deletes the lock file",
        "getLockInfo returns session_id and acquired_at from lock file content",
        "clearStaleLocks removes all .lock files in the board's locks directory and returns count",
        "All 5 lock tests pass: 'npm test --workspace=cli -- --run lock'"
      ],
      "passes": true
    },
    {
      "id": "LIB-6",
      "category": "CLI Libraries",
      "description": "Implement agent.ts module with spawnInteractive (stdio inherited), spawnPrintMode (captures stdout, detects <ralfie>COMPLETE</ralfie>), and generateSessionId",
      "steps_to_verify": [
        "spawnInteractive spawns the configured agent_command with inherited stdio and returns exit code",
        "spawnPrintMode spawns agent with '-p' flag, captures stdout, and sets result.complete=true when output contains '<ralfie>COMPLETE</ralfie>'",
        "generateSessionId returns a string matching pattern 'ralfie-<timestamp>-<random>'"
      ],
      "passes": true
    },
    {
      "id": "LIB-7",
      "category": "CLI Libraries",
      "description": "Implement skills.ts module and create three bundled skill markdown files: ralfie-plan.md, ralfie-edit.md, ralfie-run.md with installSkills function",
      "steps_to_verify": [
        "cli/src/skills/ralfie-plan.md exists with frontmatter (name, description) and phases: Grilling, Plan Generation, PRD Generation",
        "cli/src/skills/ralfie-edit.md exists with phases: Setup (reads board state), Grilling, Update, Drift Log",
        "cli/src/skills/ralfie-run.md exists with workflow: Pick Task, Claim Item, Implement, Run Feedback Loops, Update Progress, Update PRD, Commit, Check Completion, Failure Protocol",
        "installSkills copies all 3 skill files from bundled source to .claude/skills/ directory"
      ],
      "passes": true
    },
    {
      "id": "CMD-1",
      "category": "CLI Commands",
      "description": "Implement 'ralf init' command that creates .ralfie/ directory structure, writes default config, and installs Claude Code skills",
      "steps_to_verify": [
        "Running 'ralf init' creates .ralfie/ directory, .ralfie/config.json, and .ralfie/boards/",
        "Running 'ralf init' installs skills to .claude/skills/ralfie-plan.md, ralfie-edit.md, ralfie-run.md",
        "Running 'ralf init' twice does not overwrite modified config.json",
        "Both init tests pass: 'npm test --workspace=cli -- --run init'"
      ],
      "passes": true
    },
    {
      "id": "CMD-2",
      "category": "CLI Commands",
      "description": "Implement CLI entry point (cli/src/index.ts) with Commander program definition, 'ralf' name, version 0.0.1, and init command wired",
      "steps_to_verify": [
        "cli/src/index.ts imports Commander and defines program with name 'ralf'",
        "Running 'node cli/dist/index.js --help' shows available commands",
        "Running 'node cli/dist/index.js init' executes the init command"
      ],
      "passes": true
    },
    {
      "id": "CMD-3",
      "category": "CLI Commands",
      "description": "Implement 'ralf plan' command that verifies skill installation and spawns interactive Claude Code session with --prompt to invoke /ralfie-plan skill",
      "steps_to_verify": [
        "Running 'ralf plan' without prior 'ralf init' exits with error about missing skills",
        "Command spawns Claude Code in interactive mode (stdio inherited) with initial prompt referencing /ralfie-plan skill"
      ],
      "passes": true
    },
    {
      "id": "CMD-4",
      "category": "CLI Commands",
      "description": "Implement 'ralf edit <board>' command that verifies board exists and skill installed, then spawns interactive Claude Code session with /ralfie-edit skill for the specified board",
      "steps_to_verify": [
        "Running 'ralf edit nonexistent-board' exits with error about board not found",
        "Command spawns Claude Code in interactive mode with prompt referencing the board path and /ralfie-edit skill"
      ],
      "passes": true
    },
    {
      "id": "CMD-5",
      "category": "CLI Commands",
      "description": "Implement 'ralf run <board> [iterations]' command that runs serial agent loop: spawns Claude Code in print mode per iteration, detects COMPLETE signal, respects max iterations from config or CLI arg",
      "steps_to_verify": [
        "Running 'ralf run nonexistent-board' exits with error",
        "Command uses default_iterations from config when no iterations argument provided",
        "Each iteration sends prompt with @prd.json, @progress.md, @plan.md references and session ID",
        "Loop exits early when agent output contains '<ralfie>COMPLETE</ralfie>'",
        "Loop prints iteration counter (e.g., 'Iteration 3/10') for each iteration"
      ],
      "passes": true
    },
    {
      "id": "CMD-6",
      "category": "CLI Commands",
      "description": "Implement 'ralf list' command that displays all boards with progress bars showing done/total counts, active count, and failed count",
      "steps_to_verify": [
        "Output includes board name, visual progress bar, done/total count, active count, and failed count",
        "Empty state shows 'No boards found' message",
        "List test passes: 'npm test --workspace=cli -- --run list'"
      ],
      "passes": true
    },
    {
      "id": "CMD-7",
      "category": "CLI Commands",
      "description": "Implement 'ralf status <board> <status>' command that filters and displays PRD items by status (pending, in_progress, done, failed, verified)",
      "steps_to_verify": [
        "Filters items correctly — only items matching the specified status are shown",
        "Each item displays id, category, description, assigned agent (if any), and comment count",
        "Invalid status value shows error with valid options listed",
        "Status test passes: 'npm test --workspace=cli -- --run status'"
      ],
      "passes": true
    },
    {
      "id": "CMD-8",
      "category": "CLI Commands",
      "description": "Implement 'ralf verify <board> <item-id>' command that marks a done item as verified in the PRD",
      "steps_to_verify": [
        "Verifying a 'done' item changes its status to 'verified'",
        "Verifying a non-'done' item (e.g., pending) exits with error",
        "Verifying on a nonexistent board exits with error",
        "Verify test passes: 'npm test --workspace=cli -- --run verify'"
      ],
      "passes": true
    },
    {
      "id": "CMD-9",
      "category": "CLI Commands",
      "description": "Implement 'ralf unlock <board>' command that clears all lockfiles for a board",
      "steps_to_verify": [
        "All .lock files in the board's locks/ directory are deleted",
        "Output shows count of cleared locks",
        "Unlock test passes: 'npm test --workspace=cli -- --run unlock'"
      ],
      "passes": true
    },
    {
      "id": "SERVER-1",
      "category": "Server",
      "description": "Implement file watcher (cli/src/server/watcher.ts) that watches .ralfie/boards/ for file changes and invokes callback with event type, board name, and filename",
      "steps_to_verify": [
        "Watches boards directory for new board creation events",
        "Watches each existing board directory for prd.json, progress.md, plan.md changes",
        "Watches locks/ subdirectory for lock file creation/deletion",
        "Returns array of FSWatcher instances for cleanup"
      ],
      "passes": true
    },
    {
      "id": "SERVER-2",
      "category": "Server",
      "description": "Implement WebSocket server (cli/src/server/ws.ts) with createWsServer and broadcast function that sends WsEvent JSON to all connected clients",
      "steps_to_verify": [
        "createWsServer attaches WebSocketServer to the HTTP server instance",
        "broadcast sends JSON-serialized WsEvent to all clients with readyState OPEN",
        "WsEvent includes type, board, data, and timestamp fields"
      ],
      "passes": true
    },
    {
      "id": "SERVER-3",
      "category": "Server",
      "description": "Implement REST API (cli/src/server/api.ts) with endpoints: GET /api/boards, GET /api/boards/:name, POST /api/boards/:name/verify/:itemId, POST /api/boards/:name/run, GET /api/config, PUT /api/config",
      "steps_to_verify": [
        "GET /api/boards returns array of all boards with PRD and progress data",
        "GET /api/boards/:name returns full board data (meta, plan, prd, progress)",
        "GET /api/boards/:name returns 404 for nonexistent board",
        "POST /api/boards/:name/verify/:itemId verifies the item and returns {ok: true}",
        "POST /api/boards/:name/run accepts {iterations} body and starts a background run",
        "GET /api/config returns current RalfieConfig",
        "PUT /api/config accepts new config JSON and persists it"
      ],
      "passes": true
    },
    {
      "id": "SERVER-4",
      "category": "Server",
      "description": "Implement HTTP static file server (cli/src/server/http.ts) that serves React UI build from cli/dist/ui/ with SPA fallback to index.html",
      "steps_to_verify": [
        "Serves static files from cli/dist/ui/ with correct MIME types",
        "Routes API requests to the API handler before static file serving",
        "Non-file routes fall back to index.html for SPA client-side routing",
        "Returns 404 with helpful message if UI build is missing"
      ],
      "passes": true
    },
    {
      "id": "SERVER-5",
      "category": "Server",
      "description": "Implement 'ralf serve' command that starts HTTP+WebSocket server, wires file watcher to broadcast WebSocket events, listens on configured port",
      "steps_to_verify": [
        "Server starts on the port from config (default 3333)",
        "File changes in .ralfie/ trigger appropriate WebSocket event types (prd:updated, progress:updated, board:updated, lock:acquired/released)",
        "Console output shows URLs for HTTP and WebSocket endpoints"
      ],
      "passes": true
    },
    {
      "id": "UI-1",
      "category": "UI Shell",
      "description": "Create React app entry point with index.html, main.tsx (StrictMode + BrowserRouter), index.css (Tailwind import + dark theme CSS variables), and App.tsx with route definitions",
      "steps_to_verify": [
        "ui/index.html has root div and script tag for main.tsx",
        "ui/src/main.tsx renders App inside StrictMode and BrowserRouter",
        "ui/src/index.css imports Tailwind and defines CSS variables: --bg, --bg-card, --border, --text, --text-muted, --accent, --success, --warning, --danger",
        "App.tsx defines routes: / (Dashboard), /boards (BoardList), /boards/:name (BoardDetail), /settings (Settings)"
      ],
      "passes": true
    },
    {
      "id": "UI-2",
      "category": "UI Shell",
      "description": "Create API client (ui/src/lib/api.ts) with fetch wrappers: fetchBoards, fetchBoard, verifyItem, triggerRun, fetchConfig, updateConfig",
      "steps_to_verify": [
        "fetchBoards calls GET /api/boards and returns parsed JSON",
        "fetchBoard(name) calls GET /api/boards/:name",
        "verifyItem(board, itemId) calls POST /api/boards/:board/verify/:itemId",
        "triggerRun(board, iterations) calls POST /api/boards/:board/run with JSON body",
        "fetchConfig and updateConfig call GET/PUT /api/config respectively"
      ],
      "passes": true
    },
    {
      "id": "UI-3",
      "category": "UI Shell",
      "description": "Create WebSocket hook (ui/src/lib/ws.ts) with useWs that auto-connects, parses WsEvent JSON messages, auto-reconnects after 2s on disconnect, and exposes connected state",
      "steps_to_verify": [
        "useWs accepts an onEvent callback and calls it with parsed WsEvent on each message",
        "WebSocket auto-reconnects after 2-second delay on close",
        "Hook returns { connected: boolean } state",
        "Uses ref for callback to avoid reconnection on callback changes"
      ],
      "passes": true
    },
    {
      "id": "UI-4",
      "category": "UI Shell",
      "description": "Create Layout component with sidebar navigation (Dashboard, Boards, Settings) and main content area, using monospace font and dark theme",
      "steps_to_verify": [
        "Sidebar is 56px (w-56) wide with border-right",
        "Nav items highlight with accent color when active (matching current route)",
        "Main content area is scrollable and has padding",
        "'ralfie' title appears at top of sidebar"
      ],
      "passes": true
    },
    {
      "id": "UI-5",
      "category": "UI Pages",
      "description": "Implement DashboardPage with aggregated stats across all boards, items-per-day bar chart (last 7 days), and board overview cards with progress bars — auto-refreshes via WebSocket",
      "steps_to_verify": [
        "Displays StatsCards showing total, completed, in-progress, failed, and verified item counts",
        "Displays ItemsPerDayChart bar chart using recharts with last 7 days of activity",
        "Lists all boards as cards with name, done/total count, percentage, and visual progress bar",
        "Board cards link to /boards/:name detail page",
        "Data refreshes automatically when WebSocket events are received"
      ],
      "passes": true
    },
    {
      "id": "UI-6",
      "category": "UI Pages",
      "description": "Implement BoardListPage showing all boards with status breakdown (pending/active/done/failed/verified counts) — auto-refreshes via WebSocket",
      "steps_to_verify": [
        "Each board displays name, description, total item count, and per-status counts",
        "Board cards link to /boards/:name detail page",
        "Empty state shows message directing user to run 'ralf plan'",
        "Data refreshes on WebSocket events"
      ],
      "passes": true
    },
    {
      "id": "UI-7",
      "category": "UI Pages",
      "description": "Implement BoardDetailPage with tabbed view (PRD Items kanban, Plan markdown viewer, Progress timeline), Run button with dialog, and verify button on done items — auto-refreshes via WebSocket on matching board events",
      "steps_to_verify": [
        "Three tabs: 'PRD Items' (kanban), 'Plan' (markdown), 'Progress' (markdown timeline)",
        "Kanban view shows 5 columns: Pending, In Progress, Done, Failed, Verified",
        "Each kanban card shows item ID, category, description, assigned agent, and comment count",
        "Done items have a 'Verify' button that calls the verify API and refreshes",
        "Run button opens RunDialog with iteration count input",
        "Plan tab renders plan.md as formatted markdown",
        "Progress tab renders progress.md as formatted markdown",
        "Only refreshes when WebSocket events match the current board name"
      ],
      "passes": true
    },
    {
      "id": "UI-8",
      "category": "UI Pages",
      "description": "Implement SettingsPage with form for editing RalfieConfig: agent_command (text), default_iterations (number), serve_port (number), feedback_loops (textarea, one per line) with save button and confirmation",
      "steps_to_verify": [
        "Loads current config on mount via fetchConfig",
        "Displays input fields for agent_command, default_iterations, serve_port",
        "Feedback loops shown as textarea with one command per line",
        "Save button calls PUT /api/config and shows 'Saved!' confirmation for 2 seconds"
      ],
      "passes": true
    },
    {
      "id": "UI-9",
      "category": "UI Components",
      "description": "Implement StatsCards component displaying 5 KPI cards (Total Items, Completed, In Progress, Failed, Verified) with color-coded values",
      "steps_to_verify": [
        "Renders 5 cards in a grid layout",
        "Each card shows label and value with appropriate color (success for completed/verified, accent for in-progress, danger for failed)",
        "Cards have dark bg-card background with border styling"
      ],
      "passes": true
    },
    {
      "id": "UI-10",
      "category": "UI Components",
      "description": "Implement ItemsPerDayChart component using recharts BarChart showing items with activity per day for last 7 days",
      "steps_to_verify": [
        "Renders a bar chart using recharts ResponsiveContainer",
        "X-axis shows MM-DD date labels",
        "Y-axis shows integer counts (no decimals)",
        "Bars use accent color (#6366f1) with rounded top corners"
      ],
      "passes": true
    },
    {
      "id": "UI-11",
      "category": "UI Components",
      "description": "Implement PrdKanban component with 5 status columns, item cards showing ID/category/description/agent/comments, and verify button on done items",
      "steps_to_verify": [
        "5 columns rendered: Pending, In Progress, Done, Failed, Verified with color-coded headers",
        "Each column header shows item count",
        "Item cards display ID (monospace bold), category, description, assigned agent (if any), and comment count",
        "Done items have a verify button that calls onVerify callback with item ID"
      ],
      "passes": true
    },
    {
      "id": "UI-12",
      "category": "UI Components",
      "description": "Implement PlanViewer and ProgressTimeline components that render markdown content using react-markdown with dark prose styling",
      "steps_to_verify": [
        "PlanViewer renders markdown string as formatted HTML with prose-invert styling",
        "ProgressTimeline renders markdown or shows 'No progress logged yet' when empty",
        "Both components wrap content in bg-card styled container"
      ],
      "passes": true
    },
    {
      "id": "UI-13",
      "category": "UI Components",
      "description": "Implement RunDialog modal component with iteration count input, Start Run button that calls triggerRun API, and success confirmation state",
      "steps_to_verify": [
        "Modal overlay appears when open=true, dismisses on backdrop click or Cancel",
        "Input field for max iterations with default value 10",
        "Start Run button calls triggerRun API with board name and iteration count",
        "After starting, shows success message and Close button (replacing the form)"
      ],
      "passes": true
    },
    {
      "id": "BUILD-1",
      "category": "Build Pipeline",
      "description": "Configure CLI build pipeline: tsc compilation, copy skill .md files to dist/skills/, copy UI build to dist/ui/, prepend #!/usr/bin/env node shebang to dist/index.js",
      "steps_to_verify": [
        "cli/package.json has postbuild script that copies skills, UI dist, and adds shebang",
        "After build, cli/dist/skills/ contains ralfie-plan.md, ralfie-edit.md, ralfie-run.md",
        "After build, cli/dist/ui/ contains the Vite React build output",
        "cli/dist/index.js starts with '#!/usr/bin/env node'",
        "cli/dist/index.js is executable (chmod +x)"
      ],
      "passes": true
    },
    {
      "id": "BUILD-2",
      "category": "Build Pipeline",
      "description": "Configure root build order: shared builds first, then UI, then CLI (so CLI can copy UI dist)",
      "steps_to_verify": [
        "Root package.json build script runs workspaces in order: shared → ui → cli",
        "Root test script builds shared first before running CLI tests",
        "Running 'npm run build' from root produces dist/ in all three workspaces"
      ],
      "passes": true
    },
    {
      "id": "E2E-1",
      "category": "Integration",
      "description": "End-to-end smoke test: clean build, ralf init creates expected files, ralf list shows empty state, ralf serve responds with HTML, all tests pass",
      "steps_to_verify": [
        "After clean build, 'ralf init' creates .ralfie/config.json with defaults, .ralfie/boards/ directory, and .claude/skills/ with 3 skill files",
        "'ralf list' shows 'No boards found' message",
        "'ralf serve' starts and responds to HTTP requests with React app HTML",
        "'npm test' passes all test suites (config, prd, board, lock, init, list, status, verify, unlock)"
      ],
      "passes": true
    },
    {
      "id": "DOCS-1",
      "category": "Documentation",
      "description": "Update CLAUDE.md with project description, architecture overview, build/run commands, test commands, and coding conventions",
      "steps_to_verify": [
        "CLAUDE.md includes project description for Ralfie",
        "CLAUDE.md documents build commands: npm run build, npm test, npm run typecheck",
        "CLAUDE.md describes monorepo structure: cli/, ui/, shared/ workspaces",
        "CLAUDE.md lists available CLI commands: init, plan, edit, run, list, status, verify, unlock, serve"
      ],
      "passes": true
    },
    {
      "id": "CONFIG-1",
      "category": "Config Options",
      "description": "Add 'effort' (low/medium/high) and 'model' (opus/sonnet/haiku) fields to RalfieConfig in shared types, with exported union types EffortLevel and AgentModel. Update defaultConfig to effort='medium', model='opus'.",
      "steps_to_verify": [
        "shared/src/types.ts exports EffortLevel type as 'low' | 'medium' | 'high'",
        "shared/src/types.ts exports AgentModel type as 'opus' | 'sonnet' | 'haiku'",
        "RalfieConfig interface includes effort: EffortLevel and model: AgentModel fields",
        "defaultConfig in cli/src/lib/config.ts has effort='medium' and model='opus'",
        "'npm run build' succeeds with no type errors",
        "Existing tests pass: 'npm test'"
      ],
      "passes": true
    },
    {
      "id": "CONFIG-2",
      "category": "Config Options",
      "description": "Create cli/src/lib/claude-settings.ts with syncClaudeSettings function that reads .ralfie/config.json effort and model values, maps them to Claude Code settings (effortLevel and model identifier), and merges into .claude/settings.json without clobbering existing settings.",
      "steps_to_verify": [
        "syncClaudeSettings reads effort from ralfie config and writes effortLevel to .claude/settings.json",
        "syncClaudeSettings maps model names: opus -> claude-opus-4-6, sonnet -> claude-sonnet-4-6, haiku -> claude-haiku-4-5-20251001",
        "Existing .claude/settings.json fields are preserved when merging",
        "If .claude/settings.json does not exist, it is created",
        "syncClaudeSettings test passes: 'npm test --workspace=cli -- --run claude-settings'"
      ],
      "passes": true
    },
    {
      "id": "CONFIG-3",
      "category": "Config Options",
      "description": "Call syncClaudeSettings before the agent loop in both ralf run (cli command) and runInBackground (server API) so that effort and model are applied before each run.",
      "steps_to_verify": [
        "cli/src/commands/run.ts calls syncClaudeSettings() before entering the iteration loop",
        "cli/src/server/api.ts runInBackground calls syncClaudeSettings() before spawning agents",
        ".claude/settings.json is updated with current effort and model before the first agent is spawned"
      ],
      "passes": true
    },
    {
      "id": "CONFIG-4",
      "category": "Config Options",
      "description": "Add effort and model select dropdowns to the Settings page in the UI. Effort options: Low, Medium, High. Model options: Opus, Sonnet, Haiku. Saving writes to .ralfie/config.json via PUT /api/config.",
      "steps_to_verify": [
        "SettingsPage renders an Effort Level select dropdown with options Low, Medium, High",
        "SettingsPage renders a Model select dropdown with options Opus, Sonnet, Haiku",
        "Dropdowns reflect the current config values on page load",
        "Saving triggers PUT /api/config with the updated effort and model values",
        "PUT /api/config handler calls syncClaudeSettings() after writing config"
      ],
      "passes": true
    },
    {
      "id": "STOP-1",
      "category": "Stop Command",
      "description": "Add path helpers runsDir and runPidPath to paths.ts. Create cli/src/lib/run-tracker.ts with saveRunPid, removeRunPid, getActiveRuns, and countActiveRuns. PID files stored at .ralfie/boards/{board}/runs/{sessionId}.pid containing JSON with pid and sessionId.",
      "steps_to_verify": [
        "runsDir returns '.ralfie/boards/{boardName}/runs'",
        "runPidPath returns '.ralfie/boards/{boardName}/runs/{sessionId}.pid'",
        "saveRunPid creates the runs directory and writes a PID file with {pid, sessionId} JSON",
        "removeRunPid deletes the PID file, silently ignores if missing",
        "getActiveRuns reads all .pid files, checks if PIDs are alive via process.kill(pid, 0), returns array of {sessionId, pid}, and cleans up stale PID files for dead processes",
        "countActiveRuns returns the count from getActiveRuns",
        "run-tracker tests pass: 'npm test --workspace=cli -- --run run-tracker'"
      ],
      "passes": true
    },
    {
      "id": "STOP-2",
      "category": "Stop Command",
      "description": "Integrate PID tracking into ralf run and runInBackground. Save process.pid at run start, remove on exit (including error paths). The ralf run process is the tracked PID; child claude processes share the process group.",
      "steps_to_verify": [
        "ralf run creates a .pid file in .ralfie/boards/{board}/runs/ at start",
        "PID file is removed when run completes normally",
        "PID file is removed when run exits due to error",
        "runInBackground in the server also creates and cleans up PID files",
        "Multiple concurrent runs on the same board each create their own PID file keyed by sessionId"
      ],
      "passes": true
    },
    {
      "id": "STOP-3",
      "category": "Stop Command",
      "description": "Add releaseSessionLocks to lock.ts that finds and deletes all lock files held by a given sessionId. Add resetSessionItems to prd.ts that resets in_progress items assigned to a sessionId back to pending status with assigned_to set to null.",
      "steps_to_verify": [
        "releaseSessionLocks reads all .lock files, parses each, deletes those matching the given sessionId, returns count released",
        "resetSessionItems reads the PRD, finds items with status in_progress and assigned_to matching sessionId, sets them to pending with assigned_to=null, writes PRD back",
        "resetSessionItems does direct read-modify-write without per-item locking (safe because owning processes are already dead)",
        "Tests pass for both functions"
      ],
      "passes": true
    },
    {
      "id": "STOP-4",
      "category": "Stop Command",
      "description": "Implement ralf stop <board> CLI command. Reads all PID files for the board, sends SIGTERM to the process group (-pid), waits up to 5 seconds, sends SIGKILL if still alive, releases locks held by those sessions, resets their in_progress items to pending, deletes PID files, prints summary.",
      "steps_to_verify": [
        "ralf stop <board> kills all running agents for the specified board",
        "SIGTERM is sent to the process group (negative PID) so child claude processes also die",
        "After 5 second timeout, SIGKILL is sent if process is still alive",
        "Locks held by stopped sessions are released",
        "PRD items assigned to stopped sessions and in in_progress status are reset to pending",
        "PID files for stopped sessions are deleted",
        "Output shows summary: N processes stopped, N locks released, N items reset",
        "If no runs are active, prints 'No active runs for board {name}'",
        "ralf stop registered in cli/src/index.ts"
      ],
      "passes": true
    },
    {
      "id": "STOP-5",
      "category": "Stop Command",
      "description": "Add POST /api/boards/:name/stop endpoint that performs the same stop logic as the CLI command. Returns {ok: true, stopped: number}. Broadcasts run:stopped WebSocket event. Add run:stopped to WsEventType.",
      "steps_to_verify": [
        "POST /api/boards/:name/stop returns {ok: true, stopped: N} with count of processes killed",
        "Returns 404 if board does not exist",
        "Broadcasts run:stopped WebSocket event with board name after stopping",
        "run:stopped added to WsEventType union in shared/src/types.ts",
        "Reuses the same stop logic as the CLI command (shared function, not duplicated)"
      ],
      "passes": true
    },
    {
      "id": "STOP-6",
      "category": "Stop Command",
      "description": "Add BoardWithStatus type extending Board with activeRuns: number. Update GET /api/boards and GET /api/boards/:name to return BoardWithStatus using countActiveRuns from run-tracker.",
      "steps_to_verify": [
        "BoardWithStatus interface in shared/src/types.ts extends Board with activeRuns: number",
        "GET /api/boards returns array of BoardWithStatus with activeRuns count per board",
        "GET /api/boards/:name returns BoardWithStatus with activeRuns count",
        "activeRuns reflects actual live process count (stale PIDs for dead processes are not counted)"
      ],
      "passes": true
    },
    {
      "id": "STOP-7",
      "category": "Stop Command",
      "description": "Add stop button to BoardDetailPage that appears when activeRuns > 0. Button shows 'Stop (N)' with agent count, styled with danger color. Clicking calls POST /api/boards/:name/stop. Add stopBoard function to ui/src/lib/api.ts.",
      "steps_to_verify": [
        "Stop button appears next to Run button only when activeRuns > 0",
        "Button text shows 'Stop (N)' where N is the number of active runs",
        "Button is styled with danger/red color (var(--danger)) to distinguish from Run",
        "Clicking the button calls stopBoard API and the page refreshes via WebSocket run:stopped event",
        "stopBoard function in api.ts calls POST /api/boards/:name/stop and returns response"
      ],
      "passes": true
    },
    {
      "id": "STOP-8",
      "category": "Stop Command",
      "description": "Add active run indicator to BoardListPage. Show 'N agents running' badge next to boards with activeRuns > 0 using the BoardWithStatus API response.",
      "steps_to_verify": [
        "Boards with activeRuns > 0 display an 'N agents running' badge",
        "Badge uses accent color styling to indicate activity",
        "Boards with no active runs do not show the badge",
        "Badge updates in real-time via WebSocket events"
      ],
      "passes": true
    },
    {
      "id": "INIT-1",
      "category": "Init Permissions",
      "description": "Update ralf init to merge Claude Code permissions into .claude/settings.json. Ensures permissions.allow includes ['Bash', 'Edit', 'Write', 'Read']. Also writes default effort and model settings via syncClaudeSettings. If .claude/settings.json already exists, merges without clobbering existing permissions or settings.",
      "steps_to_verify": [
        "ralf init creates .claude/settings.json with permissions.allow containing ['Bash', 'Edit', 'Write', 'Read']",
        "ralf init sets effortLevel and model in .claude/settings.json based on ralfie config defaults",
        "If .claude/settings.json exists with additional permissions (e.g. 'WebSearch'), they are preserved after init",
        "If .claude/settings.json exists with other settings (e.g. theme), they are preserved after init",
        "Init output lists .claude/settings.json in the created/updated files",
        "Running ralf init twice produces the same result (idempotent)"
      ],
      "passes": true
    },
    {
      "id": "E2E-2",
      "category": "Integration",
      "description": "End-to-end verification: full build passes, all tests pass, typecheck passes, lint passes. ralf init creates .claude/settings.json with permissions and default effort/model. Config file has effort and model fields.",
      "steps_to_verify": [
        "'npm run build' succeeds across all workspaces",
        "'npm test' passes all test suites including new tests for claude-settings, run-tracker, stop",
        "'npm run typecheck' passes with no errors",
        "'npm run lint' passes with no errors",
        "After ralf init, .claude/settings.json contains permissions.allow with Bash, Edit, Write, Read",
        "After ralf init, .ralfie/config.json contains effort='medium' and model='opus'"
      ],
      "passes": true
    }
  ]
}
